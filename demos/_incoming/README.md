# Drop generated images here

Put every downloaded Higgsfield image in this folder — no renaming needed.
Each filename carries its job id, so the build script identifies what it is
and moves it into the right demo folder itself.
